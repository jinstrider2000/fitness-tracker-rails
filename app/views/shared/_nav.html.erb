<nav class="navbar navbar-default">
	<div class="container-fluid">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main_nav" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="/">Fitness Tracker</a>
		</div>
		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="main_nav">
			<ul class="nav navbar-nav navbar-left">
				<% if user_signed_in? %>
					<li class="<%=link_status("activity-feed")%>"><a href="<%="/activity-feed"%>">Activity Feed<%="<span class=\"sr-only\"> (current)</span>" if link_status("activity-feed") == "active"%></a></li>
					<li class="dropdown <%=link_status("achievements")%>">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Achievement<%="<span class=\"sr-only\"> (current)</span>" if link_status("achievements") == "active"%><span class="caret vis-sm-inline"></span></a>
						<ul class="dropdown-menu">
							<li><a href="<%=new_achievement_path%>%">New Achievement</a></li>
							<li role="separator" class="divider"></li>
							<li><a href="<%=user_achievements_path(current_user)%>">My Achievements</a></li>
						</ul>
					</li>
					<li class="dropdown <%=link_status("exercises")%>">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Exercise<%="<span class=\"sr-only\"> (current)</span>" if link_status("exercises") == "active"%><span class="caret vis-sm-inline"></span></a>
						<ul class="dropdown-menu">
							<li><a href="<%=user_exercises_path(current_user)%>">My Exercises</a></li>
						</ul>
					</li>
					<li class="dropdown <%=link_status("foods")%>">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Food<%="<span class=\"sr-only\"> (current)</span>" if link_status("foods") == "active"%><span class="caret vis-sm-inline"></span></a>
						<ul class="dropdown-menu">
							<li><a href="<%=user_foods_path(current_user)%>">My Foods</a></li>
						</ul>
					</li>
				<% end %>
			</ul>
			<ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
					<li><img class="img-circle mini-pic-display" src="<%=profile_pic_path(current_user)%>" alt="Profile Pic"></li>
					<li class="dropdown">
						<a id="acct-dropdown" href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%=current_user.first_name%><span class="caret"></span></a>
						<ul class="dropdown-menu">
							<li><a href="<%=user_path(current_user)%>">My Profile</a></li>
							<li role="separator" class="divider"></li>
							<li><a href="<%=destroy_user_session_path%>">Log Out</a></li>
						</ul>
					</li>
				<% else %>
				<!--
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">My Account<span class="caret"></span></a>
						<ul class="dropdown-menu">
							<li><a href="<%=new_user_session_path%>%">Log In</a></li>
						</ul>
					</li> -->
					<li><a href="<%=new_user_session_path%>">Log In</a></li>
				<% end %>
			</ul>
		</div><!-- /.navbar-collapse -->
	</div><!-- /.container-fluid -->
</nav>